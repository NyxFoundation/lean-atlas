import { GraphCanvas } from "@/components/graph/GraphCanvas";

/**
 * LeanAtlas Demo Page
 *
 * This demo shows the dependency graph viewer using data from the
 * XMSS Encoding Formal Verification project.
 */
export default function DemoPage() {
  return <GraphCanvas />;
}
